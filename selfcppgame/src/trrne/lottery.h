#ifndef LOTTERY_HPP
#define LOTTERY_HPP

#include <bits/stdc++.h>
#include "rnd.h"

using namespace std;

namespace trrne
{
    class lottery
    {
    public:
        static int bst(double *weights_arr, int length)
        {
            if (length <= 0)
            {
                throw exception();
            }

            double weights[length]{};
            return 0;
        }
    };
}

#endif // LOTTERY_HPP